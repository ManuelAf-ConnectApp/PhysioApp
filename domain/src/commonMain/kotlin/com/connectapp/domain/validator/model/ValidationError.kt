package com.connectapp.domain.validator.model

enum class ValidationError {
    FIELD_EMPTY,
    TOO_SHORT,
    INVALID_EMAIL,
    INVALID_PHONE,
    INVALID_DNI,
    INVALID_DNI_LETTER,
    INVALID_PASSWORD,
    PASSWORDS_NOT_MATCH
}
